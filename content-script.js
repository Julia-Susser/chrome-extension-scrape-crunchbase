var nextSpan = document.querySelector('.next-content span');

// Check if the span exists and its text is 'Next', then click it
if (nextSpan && nextSpan.textContent === "Next") {
    nextSpan.click();
}